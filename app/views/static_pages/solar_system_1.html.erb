<body></body>
<script src="http://gamingJS.com/Three.js"></script>
<script src="http://gamingJS.com/ChromeFixes.js"></script>
<script>
  // This is where stuff in our game will happen:
  var scene = new THREE.Scene();

  // This is what sees the stuff:
  var aspect_ratio = window.innerWidth / window.innerHeight;
  var camera = new THREE.PerspectiveCamera(75, aspect_ratio, 1, 10000);
  camera.position.z = 400;
  camera.position.y = 0;
  scene.add(camera);

  // This will draw what the camera sees onto the screen:
  var renderer = new THREE.CanvasRenderer();
  renderer.setSize(window.innerWidth, window.innerHeight);
  document.body.appendChild(renderer.domElement);

  // ******** START CODING ON THE NEXT LINE ********
    
    // sun
    var shape = new THREE.SphereGeometry(100, 50, 50);
    var cover = new THREE.MeshPhongMaterial({color:0xFFFF00}); 
    var ball = new THREE.Mesh(shape, cover);
    scene.add(ball);
    // ball.position.set(-300,250,-300);
    
     
    // var shape = new THREE.CubeGeometry(100,100,100);
    // var cover = new THREE.MeshNormalMaterial();
    // var cube = new THREE.Mesh(shape, cover);
    // scene.add(cube);
    // cube.rotation.set(0.5,0.5,0);
    // cube.position.set(300,300,-300);
    
    // var shape = new THREE.CylinderGeometry(100,100,10,50);
    // var cover = new THREE.MeshNormalMaterial();
    // var cylinder = new THREE.Mesh(shape, cover);
    // scene.add(cylinder);
    // cylinder.rotation.set(5,0,2);
    // cylinder.position.set(-250, -250, -250);
    
    // var shape = new THREE.PlaneGeometry(300, 100); 
    // var cover = new THREE.MeshNormalMaterial(); 
    // var ground = new THREE.Mesh(shape, cover); scene.add(ground);
    // ground.rotation.set(0.5, 0, 0);
    // ground.position.set(250, -250, -250);
    
    // ring 1
    var shape = new THREE.TorusGeometry(150, 1, 50, 50);
    var cover = new THREE.MeshNormalMaterial(); 
    var donut = new THREE.Mesh(shape, cover);
    scene.add(donut);
    donut.rotation.set(1.75,0,0)
    
    // ring 2
    var shape = new THREE.TorusGeometry(200, 1, 50, 50);
    var cover = new THREE.MeshNormalMaterial(); 
    var donut = new THREE.Mesh(shape, cover);
    scene.add(donut);
    donut.rotation.set(1.75,0,0)
    
    // ring 3
    var shape = new THREE.TorusGeometry(250, 1, 50, 50);
    var cover = new THREE.MeshNormalMaterial(); 
    var donut = new THREE.Mesh(shape, cover);
    scene.add(donut);
    donut.rotation.set(1.75,0,0)
    
    // planet 1
    var shape = new THREE.SphereGeometry(20,20, 20);
    var cover = new THREE.MeshNormalMaterial(); 
    var ball = new THREE.Mesh(shape, cover);
    scene.add(ball);
    ball.position.set(-192,-10,40);
    
    // planet 2
    var shape = new THREE.SphereGeometry(30,20, 20);
    var cover = new THREE.MeshNormalMaterial(); 
    var ball = new THREE.Mesh(shape, cover);
    scene.add(ball);
    ball.position.set(250,-10,40); 
    
    // planet 3
    var shape = new THREE.SphereGeometry(12,20, 20);
    var cover = new THREE.MeshPhongMaterial({color: 0x000099}); 
    var ball = new THREE.Mesh(shape, cover);
    scene.add(ball);
    ball.position.set(0,-20,200);
    
    //spaceship
    var shape = new THREE.CylinderGeometry(5,20, 5, 20);
    var cover = new THREE.MeshNormalMaterial(); 
    var pyramid = new THREE.Mesh(shape, cover);
    scene.add(pyramid);
    pyramid.position.set(0,-20,300);
    
    
    
  // Now, show what the camera sees on the screen:
  
  var render = function () {
  renderer.render(scene, camera);
  requestAnimationFrame(render);
  if(pyramid !== undefined){
  pyramid.position.x += rotationY;
  }
  if (pyramid !== undefined){
    pyramid.position.z += rotationX;
  }
  
  }
  
 
  
  function handleKeyDown(event) {
  if (event.keyCode === 39) { //right arrow
    rotationY = 5;
  }else if(event.keyCode === 37){ //left arrow
    rotationY = -5;
  }else if (event.keyCode === 38){ //up arrow
    rotationX = -5;
  }else if (event.keyCode === 40){ //down arrow
    rotationX = 5;
  }else if (event.keyCode === 65){
    bagzoom = -0.5;
  }else if (event.keyCode === 83){
    bagzoom = 0.5;
  }
}

function handleKeyUp(event) {
  if (event.keyCode === 39) {
    rotationY = 0;
  }else if (event.keyCode === 37){
    rotationY = 0;
  }else if (event.keyCode === 38 ){
    rotationX = 0;
  }else if (event.keyCode === 40){
    rotationX = 0;
  }else if (event.keyCode === 65){
    bagzoom = 0;
  }else if (event.keyCode === 83){
    bagzoom = 0;
  }
}

window.addEventListener('keydown', handleKeyDown, false);
window.addEventListener('keyup', handleKeyUp, false);
  
  render();
</script>
